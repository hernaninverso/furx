{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile-pairing",
  "description": "065 — escáner de QR nativo para el pairing (solo móvil).",
  "windows": ["main"],
  "platforms": ["iOS", "android"],
  "permissions": ["barcode-scanner:default"]
}
